process.container = 'quay.io/nextflow/bash'

params {
    message = "test message from top config"
    extra_message = 'extra, extra, read all about it'
}

profiles {
    foo { 
        includeConfig "foo/sub.config" 
        //params {
        //    message = "test message from subconfig"
        //}
    }
}
